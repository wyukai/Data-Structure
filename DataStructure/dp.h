#ifndef DP_H
#define DP_H
#include <vector>
class Dp
{
public:
	//1.最大连续乘积子数组问题
	double MaxProductSubstring(double *a, int length);
	double MAxProductSubstring1(double *a, int length);
	//2.计算组合中乘积最大的一组
	int MaxProductSub(int *a, int length);
	int MaxProductSub1(int *a, int length);

	/*vector<int> GetLeastNumbersSolution(vector<int> a,int k);*/

private:

};


#endif // !DP_H
#pragma once
